{
  "rules": {
    "reviews": {
      "$reviewId": {
        "allow read": "if resource.data.visible == true;",
        "allow create": "if true;",
        "allow update, delete": "if false;"
      }
    }
  }
}
