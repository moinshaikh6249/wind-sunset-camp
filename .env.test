# Test credentials for automated E2E tests.
# IMPORTANT: Use dedicated test accounts that do not have access to production data.

# A standard registered user account
TEST_USER_EMAIL="qa.e2e.user@example.com"
TEST_USER_PASSWORD="DemoPass@123"

# An admin user account (must exist with admin role in backend data)
TEST_ADMIN_EMAIL="moinshaikh6249@gmail.com"
TEST_ADMIN_PASSWORD="123321123"

# Ensure these users exist in backend auth/user data for tests to pass.
# The registration test will create its own user, but these are needed for login tests.
